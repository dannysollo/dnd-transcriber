venv/
__pycache__/
*.pyc
*.pyo
.DS_Store

# Heavy session files — keep local only
sessions/
campaigns/

# Database
*.db
*.db-shm
*.db-wal

# Temp files
*.wav
*_vad.wav

# Whisper model cache
~/.cache/whisper/
vocab.txt
.env
worker/worker.yaml

# Desktop launcher build output and unbuildable-from-here binary resources
# (PyInstaller dist, a static ffmpeg.exe, the WebView2 bootstrapper) — these
# get produced/added on the Windows machine that actually builds the .exe,
# not checked into source control.
desktop/build/dist/
desktop/build/work/
desktop/resources/
