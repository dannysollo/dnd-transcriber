faster-whisper
torch
torchaudio
requests
pyyaml
tqdm
soundfile
silero-vad
discord.py-self  # For craig_watcher.py — watches for Craig DMs
flask>=2.0        # Local web dashboard (gui_server.py)
# pyannote.audio  # Optional: speaker diarization (multi-speaker on single track)
                  # Install: pip install pyannote.audio
                  # Also accept model terms at huggingface.co/pyannote/speaker-diarization-3.1
# nemo_toolkit[asr]  # Optional: Parakeet-TDT-0.6B (feature/parakeet branch)
                     # Install: pip install nemo_toolkit[asr]  (~2GB deps, CUDA required)
                     # Model downloads on first use: nvidia/parakeet-tdt-0.6b (~1.2GB)
