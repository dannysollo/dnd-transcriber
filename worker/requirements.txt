faster-whisper
nvidia-cublas-cu12  # ctranslate2 (faster-whisper's engine) needs cuBLAS at inference time and
                    # doesn't bundle it (unlike cuDNN, which it does ship) or auto-discover it the
                    # way torch's own import machinery does for its own CUDA deps — see
                    # desktop/worker_process.py's PATH handling for how this gets found at runtime.
                    # A system-wide CUDA Toolkit install also satisfies this; this pip package is
                    # the lighter-weight alternative so onboarding doesn't require one.
nemo_toolkit[asr]  # Canary engine (canary-1b-flash / canary-1b) — CUDA required, ~2GB deps
torch
torchaudio
requests
pyyaml
tqdm
soundfile
silero-vad
flask>=2.0        # Local web dashboard (gui_server.py)
# pyannote.audio  # Optional: speaker diarization (multi-speaker on single track)
                  # Install: pip install pyannote.audio
                  # Also accept model terms at huggingface.co/pyannote/speaker-diarization-3.1
